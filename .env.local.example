# Copy to .env.local for local dev (Next.js auto-loads .env.local, which is git-ignored).
# Base URL of your deployed API Gateway stage; the frontend POSTs to /search, /summarize, /rag under this.
NEXT_PUBLIC_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod
